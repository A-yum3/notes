- ページネーションを使う際はorder byクエリを必ず入れる(順番が保証されないため)
- order byでもインデックスは使用される
- インデックスは1個しか使われない為、複合インデックスを追加する必要がある
- インデックスを付けられた順序に適応されるため、注意する
	- 例えば `$table->index(['last_name', 'first_name']);`というインデックスをつけた
	- Eloquentの方は `User::query()->orderBy('first_name')->orderBy('last_name')`だと複合インデックスが効かない